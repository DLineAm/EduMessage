<Page
    x:Class="EduMessage.Pages.ItemsPickPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EduMessage.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Text="Перетащите нужные файлы в облать ниже" 
                   Margin="12"/>

        <Rectangle Grid.Row="1"
                   Margin="12"
                   StrokeDashArray="2.0 2.0"
                   Stroke="{x:Bind RectColor}"
                   Fill="Transparent"
                   StrokeThickness="2"
                   AllowDrop="True"
                   DragLeave="Rectangle_DragLeave"
                   DragOver="Rectangle_DragOver"
                   Drop="Rectangle_Drop">
            
        </Rectangle>
        <Border AllowDrop="True">
            
        </Border>
    </Grid>
</Page>
